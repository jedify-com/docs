---
title: 'Connect'
icon: 'plug'
description: 'Add the Jedify connector in Claude, or point a hosted agent at the Jedify MCP server.'
---

Jedify's MCP server runs at `https://be.jedify.com/mcp` — there is nothing to install. Add it as a **connector in Claude**, or give a **hosted agent or app builder** the server URL and an API key. Both use the same [modes](/mcp/modes) and [authentication](/mcp/authentication).

<Note>
If your organization's Jedify is hosted in the EU, use `https://be-eu.jedify.com/mcp` wherever this page says `https://be.jedify.com/mcp`.
</Note>

## Claude

Jedify is a **custom connector** in Claude. A workspace admin adds it once and every user in the organization gets it; if your plan lets you add custom connectors yourself, the same steps apply to your own account.

<Steps>
  <Step title="Open the connector settings">
    In Claude, click **+** in the message box → **Connectors** → **Manage connectors** → **Add custom connector**.
  </Step>
  <Step title="Enter the server URL">
    Name it **Jedify** and enter the URL for the [mode](/mcp/modes) you want:

    | Mode                  | Server URL                               |
    | --------------------- | ---------------------------------------- |
    | **Asker** _(default)_ | `https://be.jedify.com/mcp`              |
    | **Editor**            | `https://be.jedify.com/mcp?mode=editor`  |
    | **Builder**           | `https://be.jedify.com/mcp?mode=builder` |

    The mode is part of the URL, so add one connector per mode you need.
  </Step>
  <Step title="Connect and sign in">
    Click **Add**, then **Connect** on the Jedify connector. Your organization's Jedify login opens; sign in and return to Claude. Sessions refresh automatically — see [Authentication](/mcp/authentication).
  </Step>
</Steps>

Once added, Jedify appears alongside Claude's other connectors wherever you use Claude — the web app, Claude Desktop, and Claude Code (run `/mcp` to see it) — for every user your admin enabled it for.

<Note>
**Already using `@jedify/mcp-auth`?** Remove the `jedify` entry from your client's MCP configuration and add the connector instead — the local proxy is no longer needed.
</Note>

To confirm the mode, ask Claude _"Which Jedify tools do you have?"_ — Editor mode includes `make_edit` and `get_graph`; Builder mode includes `create_semantic_function` and `list_semantic_functions`.

## Hosted agents and other MCP clients

For platforms that connect to an MCP server with a URL and a token — ChatGPT, no-code app builders, or any MCP client outside Claude — provide the server URL and an API key in the platform's MCP settings:

|                |                                                                                                                                          |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| **Server URL** | `https://be.jedify.com/mcp` for Asker mode. Add `?mode=editor` or `?mode=builder` for the other modes — the URL is what selects the mode |
| **API key**    | Create one in Jedify under **Settings → API Keys** — see [Authentication](/mcp/authentication)                                           |

To confirm the mode, ask your agent _"Which Jedify tools do you have?"_ — Editor mode includes `make_edit` and `get_graph`; Builder mode includes `create_semantic_function` and `list_semantic_functions`.

## Next steps

<CardGroup cols={2}>
  <Card title="Modes" icon="toggle-on" href="/mcp/modes">
    Asker, Editor, or Builder.
  </Card>
  <Card title="Authentication" icon="key" href="/mcp/authentication">
    Sign in or use an API key.
  </Card>
  <Card title="Asker examples" icon="comments" href="/mcp/asker/use-cases">
    What to ask.
  </Card>
  <Card title="Builder mode" icon="cube" href="/mcp/builder/semantic-functions">
    Build dashboards.
  </Card>
</CardGroup>
