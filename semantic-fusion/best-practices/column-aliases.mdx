---
title: "Column Aliases & Base Query Relations"
---

If a needed relation/field already exists in the base table (e.g., lead_owner), use it directly as is, no alias or external import is required.


It is recommended to only use column aliases only when you need to normalize naming for clarity or to align with business terminology.
